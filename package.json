{
  "name": "node-tests-students",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:ci": "jest --ci --coverage --reporters=default",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "coverage": "jest --coverage",
    "complexity": "eslint . --rule 'complexity:[warn,{max:10}]'"
  },
  "devDependencies": {
    "eslint": "^9.38.0",
    "jest": "^29.7.0"
  }
}
